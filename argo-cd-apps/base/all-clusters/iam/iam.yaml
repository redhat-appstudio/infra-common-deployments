---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: iam
spec:
  generators:
    - clusters:
        values:
          sourceRoot: components/iam
          environment: ""
  template:
    metadata:
      name: iam-{{nameNormalized}}
    spec:
      project: default
      source:
        path: '{{values.sourceRoot}}/{{values.environment}}'
        repoURL: https://github.com/redhat-appstudio/infra-common-deployments.git
        targetRevision: main
      destination:
        namespace: group-sync-operator
        name: in-cluster
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 50
          backoff:
            duration: 15s
