---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: codecov
spec:
  generators:
    - clusters:
        values:
          sourceRoot: components/codecov
          environment: ""
  template:
    metadata:
      name: codecov
    spec:
      project: default
      source:
        repoURL: https://github.com/redhat-appstudio/infra-common-deployments.git
        targetRevision: main
        path: '{{values.sourceRoot}}/{{values.environment}}'
      destination:
        namespace: codecov
        name: in-cluster
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: -1
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 3m
