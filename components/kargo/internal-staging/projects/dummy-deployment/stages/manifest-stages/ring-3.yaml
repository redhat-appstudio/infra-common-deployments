kind: Stage
apiVersion: kargo.akuity.io/v1alpha1
metadata:
  name: manifest-ring-3
  namespace: infra-demo
  annotations:
    kargo.akuity.io/color: green
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: dummy-deployment-manifest-wh
      sources:
        direct: false
        stages:
          - manifest-ring-2
  promotionTemplate:
    spec:
      steps:
        - task:
            name: manifest-promotion
          vars:
            - name: ring
              value: ring-3
