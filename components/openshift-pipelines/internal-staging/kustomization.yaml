apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Base resources
  - ../base

patches:
  - path: tekton-config-patch.yaml
    target:
      kind: TektonConfig
      name: config
