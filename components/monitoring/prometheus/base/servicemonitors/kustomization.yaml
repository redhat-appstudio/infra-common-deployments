apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ServiceMonitors removed - these were for applications that don't run in common clusters
# Original monitored: dora-service, pipeline-service, release-service
# TODO: Add ServiceMonitors for common cluster applications (Kargo, Kyverno, DevLake, IAM, Support Ops)
resources: []
