apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - dashboards

patches:
  - target:
      kind: GrafanaDashboard
    patch: |-
      - op: add
        path: /spec/resyncPeriod
        value: "30m"
