---
apiVersion: v1
kind: Service
metadata:
  name: support-ops-dashboard
  labels:
    app: support-ops-dashboard
spec:
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: http
  selector:
    app: support-ops-dashboard
