---
apiVersion: v1
kind: Service
metadata:
  name: support-ops-dashboard
  labels:
    app: support-ops-dashboard
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: support-ops-dashboard-tls
spec:
  ports:
    - name: https
      port: 5000
      protocol: TCP
      targetPort: https
  selector:
    app: support-ops-dashboard
