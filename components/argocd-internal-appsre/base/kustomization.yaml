apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd.yaml
  - ns.yaml
  # Wait until we have stopped the sync in the AppSRE-hosted Argo CD instance
  # - all-application-sets-app.yaml
