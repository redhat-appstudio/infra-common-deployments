apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd.yaml
  - ns.yaml
  # all-application-sets-app.yaml lives in overlays, not here, so production
  # does not inherit it via ../base. Staging opts in via
  # internal-staging/all-application-sets-app.yaml.
